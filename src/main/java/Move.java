public class Move {
    public static boolean isValidMove(Board board, Piece piece, Position newPos) {
        // Check that the newPos is in the list of all moves of the piece
        // Check that the move is within the board boundaries
        // Check that a friendly piece is not on the newPos
        // Check that the piece's own king is not in check after the move
        return false;
    }
}
